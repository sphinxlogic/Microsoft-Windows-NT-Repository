


PKTYPE(1)                USER COMMANDS                  PKTYPE(1)



NNNNAAAAMMMMEEEE
     pktype - translate a packed font file for humans to read

SSSSYYYYNNNNTTTTAAAAXXXX
     ppppkkkkttttyyyyppppeeee pk_file_name [output_file_name]

DDDDEEEESSSSCCCCRRRRIIIIPPPPTTTTIIIIOOOONNNN
     The _p_k_t_y_p_e program translates a packed font file (PK)  (out-
     put  by,  for  example,  ggggffffttttooooppppkkkk(1) to a file that humans can
     read. It also serves as a PK file-validating program  (i.e.,
     if  _p_k_t_y_p_e can read it, it's correct) and as an example of a
     PK-reading program for other software that wants to read  PK
     files.

     The _p_k__f_i_l_e__n_a_m_e on  the  command  line  must  be  complete.
     Because  the  resolution  is part of the extension, it would
     not make sense to add on a default extension as is done with
     TeX  or  DVI-reading  software.  If  no  _o_u_t_p_u_t__f_i_l_e__n_a_m_e is
     specified, the output goes to the _s_t_d_o_u_t.

     The output file gives  a  compact  encoding  of  the  packed
     encoding,  using  conventions  described in the source code.
     Run lengths of black pixels alternate with parenthesized run
     lengths  of  white pixels, and brackets are used to indicate
     when a row should be repeated.

EEEENNNNVVVVIIIIRRRROOOONNNNMMMMEEEENNNNTTTT VVVVAAAARRRRIIIIAAAABBBBLLLLEEEESSSS
     _p_k_t_y_p_e uses the environment variable PKFONTS to  search  for
     the  PK  file.   If PKFONTS is not set, it uses the variable
     TEXFONTS.  If TEXFONTS  is  not  set,  it  uses  the  system
     default:  ....::::////oooopppptttt////lllliiiibbbb////tttteeeexxxx////ffffoooonnnnttttssss.   See tttteeeexxxx(1) for the details
     of the searching.

SSSSEEEEEEEE AAAALLLLSSSSOOOO
     dvitype(1), gftopk(1), gftype(1)
     Donald Knuth et al., _M_E_T_A_F_O_N_T_w_a_r_e

AAAAUUUUTTTTHHHHOOOORRRRSSSS
     Tomas Rokicki wrote the program, and Donald Knuth originally
     ported  it  to  Unix.  It  was  published  as  part  of  the
     _M_E_T_A_F_O_N_T_w_a_r_e techical report, available from the  TeX  Users
     Group.













AT&T Bell Laboratories Last change: 8/8/90                      1



