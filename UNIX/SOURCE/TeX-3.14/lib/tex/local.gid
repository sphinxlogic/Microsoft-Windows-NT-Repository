 
               HOW TO RUN LaTeX AT SRC
 
                    30 July 1992
 
 
To run LaTeX on the file named  foo.tex  type:
 
    latex foo
 
This produces an output file named  foo.dvi .
 
To print file foo.dvi on the Epson FX-80 printer, type the following
command:
 
    dvieps foo.dvi

and
    cat foo.dvi-eps > /dev/lp0
 
 
