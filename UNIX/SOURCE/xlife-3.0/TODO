Box a region.
	Rotate box
	Flip box
	Move box
	Erase box
	Save box to #P format

Read different file formats (bitmap....) as new #-section types

lifeconv should really be made more 'useful' and convert from other formats
(pbm, x bitmap, etc).

lifeconv should pass #O, #C, and others fields through.
