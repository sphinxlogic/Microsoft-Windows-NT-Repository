


MTOOLS(1)                USER COMMANDS                  MTOOLS(1)



NNNNAAAAMMMMEEEE
     Mtools - a collection of tools for manipulating MSDOS files

SSSSYYYYNNNNOOOOPPPPSSSSIIIISSSS
     mmmmaaaattttttttrrrriiiibbbb - change MSDOS file attribute flags
     mmmmccccdddd - change MSDOS directory
     mmmmccccooooppppyyyy - copy MSDOS files to/from Unix
     mmmmddddeeeellll - delete an MSDOS file
     mmmmddddiiiirrrr - display an MSDOS directory
     mmmmffffoooorrrrmmmmaaaatttt - add an MSDOS filesystem to a low-level formatted diskette
     mmmmllllaaaabbbbeeeellll - make an MSDOS volume label
     mmmmmmmmdddd - make an MSDOS subdirectory
     mmmmrrrrdddd - remove an MSDOS subdirectory
     mmmmrrrreeeeaaaadddd - low level read (copy) an MSDOS file to Unix
     mmmmrrrreeeennnn - rename an existing MSDOS file
     mmmmttttyyyyppppeeee - display contents of an MSDOS file
     mmmmwwwwrrrriiiitttteeee - low level write (copy) a Unix file to MSDOS

DDDDEEEESSSSCCCCRRRRIIIIPPPPTTTTIIIIOOOONNNN
     Mtools is a public domain collection of  programs  to  allow
     Unix  systems  to  read,  write,  and manipulate files on an
     MSDOS  filesystem  (typically  a  diskette).   Each  program
     attempts  to emulate the MSDOS equivalent command as closely
     as practical.

     MSDOS filenames are optionally composed of  a  drive  letter
     followed  by  a colon, a subdirectory, and a filename.  Sub-
     directory names can use either the  '/'  or  '\'  separator.
     The  use  of the '\' separator or wildcards will require the
     names to be enclosed in quotes  to  protect  them  from  the
     shell.

     The regular expression "pattern  matching"  routines  follow
     the  Unix-style  rules.   For example, '*' matches all MSDOS
     files in lieu of '*.*'.  The archive, hidden, read-only  and
     system attribute bits are ignored during pattern matching.

     Not all Unix filenames are appropriate in the  MSDOS  world.
     The  Mtools commands may have to alter Unix names to fit the
     MSDOS filename restrictions.  Most commands have a -v  (ver-
     bose)  option  that  will display the new names if they have
     been changed.  The following table shows  some  examples  of
     filename conversions:

          l l l.  Unix name MSDOS name     Reason for the  change
          _    _    _  thisisatest    THISISAT  filename too long
          emmet.gray     EMMET.GRA extension       too       long
          prn.txt   XRN.TXT   PRN is a device name
           .abc     X.ABC     null                       filename
          hot+cold  HOTXCOLD  illegal character





AT&T Bell Laboratories Last change: local                       1






MTOOLS(1)                USER COMMANDS                  MTOOLS(1)



     All options use the '-'  (minus)  flag,  not  '/'  as  you'd
     expect in MSDOS.

     The _m_c_d command is used to  establish  the  device  and  the
     current  working  directory  (relative to the MSDOS filesys-
     tem), otherwise the default is assumed to be A:/.

     All the Mtools commands return 0  on  success,  1  on  utter
     failure, or 2 on partial failure.

SSSSEEEEEEEE AAAALLLLSSSSOOOO
     mattrib(1), mcd(1), mdel(1),  mformat(1),  mrd(1),  mren(1),
     mtype(1),  mcopy(1),  mdir(1),  mlabel(1), mmd(1), mread(1),
     mwrite(1)

BBBBUUUUGGGGSSSS
     An unfortunate side effect of not guessing the proper device
     (when  multiple  disk  capacities are supported) is an occa-
     sional error message from the device driver.  These  can  be
     safely ignored.



































AT&T Bell Laboratories Last change: local                       2



