


MLABEL(1)                USER COMMANDS                  MLABEL(1)



NNNNAAAAMMMMEEEE
     mlabel - make an MSDOS volume label

SSSSYYYYNNNNOOOOPPPPSSSSIIIISSSS
     mmmmllllaaaabbbbeeeellll [ ----vvvv ] drive:

DDDDEEEESSSSCCCCRRRRIIIIPPPPTTTTIIIIOOOONNNN
     _M_l_a_b_e_l displays the current volume label,  if  present,  and
     prompts  the  user  for  a  new  volume label.  To delete an
     existing volume label, press return at the prompt.

     _M_l_a_b_e_l will allow the following command line option:

     vvvv    Verbose mode.  Display the  new  volume  label  if  the
          label supplied is invalid.

     Reasonable care is taken to  create  a  valid  MSDOS  volume
     label.  If an invalid label is specified, _m_l_a_b_e_l will change
     the label (and display the new label if the verbose mode  is
     set).

     _M_l_a_b_e_l returns 0 on success or 1 on failure.

SSSSEEEEEEEE AAAALLLLSSSSOOOO
     mformat(1)






























AT&T Bell Laboratories Last change: local                       1



