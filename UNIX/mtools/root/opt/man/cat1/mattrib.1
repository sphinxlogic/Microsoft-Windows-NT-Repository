


MATTRIB(1)               USER COMMANDS                 MATTRIB(1)



NNNNAAAAMMMMEEEE
     mattrib - change MSDOS file attribute flags

SSSSYYYYNNNNOOOOPPPPSSSSIIIISSSS
     mmmmaaaattttttttrrrriiiibbbb [ ----aaaa||||++++aaaa ] [ ----hhhh||||++++hhhh ] [ ----rrrr||||++++rrrr ] [ ----ssss||||++++ssss ] msdosfile  [
     msdosfiles... ]

DDDDEEEESSSSCCCCRRRRIIIIPPPPTTTTIIIIOOOONNNN
     _M_a_t_t_r_i_b will add attribute flags to an MSDOS file (with  the
     '+' operator) or remove attribute flags (with the '-' opera-
     tor).

     _M_a_t_t_r_i_b will allow the following command line options:

     aaaa    Archive bit.  Used by some backup programs to  indicate
          a new file.

     rrrr    Read-only bit.  Used  to  indicate  a  read-only  file.
          Files  with this bit set cannot be erased by DEL.  How-
          ever, _m_d_e_l is more cunning.

     ssss    System bit.  Used by MSDOS to indicate a operating sys-
          tem file.

     hhhh    Hidden bit.  Used to make files hidden from DIR.   How-
          ever, _m_d_i_r is more cunning.

     MSDOS subdirectory names are supported with either  the  '/'
     or '\' separator.  The use of the '\' separator or wildcards
     will require the names to be enclosed in quotes  to  protect
     them from the shell.

     The _m_c_d command may be used to establish the device and  the
     current working directory (relative to MSDOS), otherwise the
     default is A:/.

     _M_a_t_t_r_i_b returns 0 on success, 1 on utter failure,  or  2  on
     partial failure.

SSSSEEEEEEEE AAAALLLLSSSSOOOO
     mcd(1)

BBBBUUUUGGGGSSSS
     Most MSDOS versions of ATTRIB don't allow this many options.











AT&T Bell Laboratories Last change: local                       1



