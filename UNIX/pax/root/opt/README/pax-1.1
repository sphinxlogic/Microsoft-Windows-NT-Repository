This directory contains source and objects for pax, a portable archive
exchange

Program:	pax
Revision:	1.1
Date ported:	13 May 1993
Ported by:	Greg Lehey, LEMIS
Compiler used:	gcc-2.3.3

Prerequisites:	none

Documentation:	man page, installed with install-sysv

Installation:	make, make install-sysv

Comments:	Comments in the file ../README.LEMIS are not repeated
		here. You should read ../README.LEMIS first.

		The Makefile recommends defining _POSIX_SOURCE.
		Unfortunately, this doesn't work as well as it should
		do, so we left it out.

Modifications:

0. configure for svr4
