c
c     maxstring = size of string heap
c     string = string heap
c     nstring = next available byte in string
c
      parameter (maxstring=10000)
      common / kstrings / strings, nstring
      character *(maxstring) strings
