slsc is a spreadsheet program.

It is based on `sc' but the user interface has been completely re-written to
make it MUCH more user friendly with Lotus style menus, Emacs bindings
(e.g., Ctrl-X Ctrl-C to quit), color coding of cells, etc.   The result is
that it no longer feels or looks like `sc'.
