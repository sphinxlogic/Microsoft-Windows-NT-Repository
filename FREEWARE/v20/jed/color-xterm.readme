rxvt is a replacement for xterm that supports colors.  It does not have the
bugs that another program called color_xterm has. 

I did not write rxvt.  However, all my programs that support color (jed,
slrn, slsc) are guaranteed to work under it.
