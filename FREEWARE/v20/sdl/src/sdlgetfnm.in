%include $fabdef;
%include $rabdef;
%include $namdef;

declare
    sdl$gt_filename character(nam$c_maxrss) varying external,
    sdl$getfnm entry (1 like fabdef, 1 like rabdef, fixed binary) returns (fixed binary);
