define verb PSI_CONVERT

  image NEW_UTIL:PSI_CONVERT

  parameter P1,
	label = INPUT_FILE,
	value (default = "SYS$MANAGER:PSIACCOUNTNG.DAT", type = $FILE)

  qualifier OUTPUT_FILE,
	value (default = "PSIACCOUNTING.FIX", type = $FILE)

