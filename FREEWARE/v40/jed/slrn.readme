slrn is an NNTP based newsreader.  It uses referenced-based threading and
draws the thread tree on terminals that support line drawing characters.
slrn also has support for color terminals.

You need to pick up the slang library distribution as well if you do not
already have it.
