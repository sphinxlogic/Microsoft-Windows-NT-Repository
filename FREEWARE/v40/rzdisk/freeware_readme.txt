RZDISK, UTILITIES, SCSI disk management utility.
 
RZDISK.EXE

    RZDISK is a SCSI disk maintenance utility which can be used to
    manipulate things like disk parameters, reassign bad blocks,
    format disks, etc. Only disks directly connected to a VAX or
    Alpha machine can be used. The utility runs on both Alpha and Vax.
