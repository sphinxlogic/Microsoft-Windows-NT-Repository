1-	set default to your architecture
	execute @tcplink [p1] [p2]
	with p1= yes if you want to disable the auditing,
	     p2=yes if you don't have decc$shr.exe
2-	an example of setup is given in tcp.com
	it loads the security filter with the sample configuration given
	in tcp_security.dat
