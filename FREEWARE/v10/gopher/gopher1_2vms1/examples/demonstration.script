$! v = 'f$verify(0)
$!
$! This demonstration script executes a directory command.
$! It turns off verify so that the script itself will not be displayed.
$!
$ dir/date gopher_root1:[gopher.vms.server]'p1
