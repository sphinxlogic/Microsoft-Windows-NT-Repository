define verb ACC_CONVERT

  image sys$sysdevice:[lederman.programs.vax.accnt]ACC_CONVERT

  parameter P1,
	label = INPUT_FILE,
	value (default = "SYS$MANAGER:ACCOUNTNG.DAT", type = $FILE)

  qualifier OUTPUT_FILE,
	value (default = "ACCOUNTING.FIX", type = $FILE)

