define verb M_TO_CSV

  image SYS$LOGIN_DEVICE:[LEDERMAN.MONITOR]MONITOR_TO_CSV.EXE

  parameter P1,
	label = INPUT_FILE,
	value (default = "MONITOR.DAT", type = $FILE)

  qualifier OUTPUT_FILE,
	value (default = "MONITOR.CSV", type = $FILE)

  qualifier SPECIAL1

